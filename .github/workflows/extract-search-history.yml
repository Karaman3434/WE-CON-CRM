name: Extract Search History

on:
  push:
    branches:
      - project-context

permissions:
  contents: write

jobs:
  extract-search-history:
    if: ${{ !contains(github.event.head_commit.message, '[refactor-bot]') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: project-context
          fetch-depth: 0

      - name: Extract search history module in place
        shell: python
        run: |
          from pathlib import Path
          import re

          index = Path('index.html')
          module = Path('assets/search-history.js')
          html = index.read_text(encoding='utf-8')
          scripts = list(re.finditer(r'<script>(.*?)</script>', html, re.DOTALL | re.IGNORECASE))
          if not scripts:
              raise SystemExit('No inline script found.')
          target = max(scripts, key=lambda m: len(m.group(1)))
          lines = target.group(1).splitlines()

          start_marker = 'function aramaGecmisiKaydet'
          end_marker = 'function hitUrunleriHesapla'
          starts = [i for i, line in enumerate(lines) if start_marker in line]
          ends = [i for i, line in enumerate(lines) if end_marker in line]
          if len(starts) != 1 or len(ends) != 1 or not starts[0] < ends[0]:
              raise SystemExit(f'Could not identify search-history boundaries: starts={starts}, ends={ends}')

          start, end = starts[0], ends[0]
          section = lines[start:end]
          if len(section) < 20:
              raise SystemExit('Search-history section unexpectedly short; refusing extraction.')

          module.parent.mkdir(parents=True, exist_ok=True)
          module.write_text('\n'.join(section).rstrip() + '\n', encoding='utf-8')

          marker = '/* SEARCH HISTORY EXTRACTED TO assets/search-history.js */'
          new_lines = lines[:start] + [marker] + lines[end:]
          new_script = '<script>\n' + '\n'.join(new_lines) + '\n</script>'
          html = html[:target.start()] + new_script + html[target.end():]
          html = html.replace(marker, '<script src="assets/search-history.js"></script>', 1)
          index.write_text(html, encoding='utf-8')
          print(f'Extracted search-history lines: {len(section)}')
          print(f'Search-history bytes: {len(module.read_bytes())}')

      - name: Validate search history extraction
        shell: python
        run: |
          from pathlib import Path
          html = Path('index.html').read_text(encoding='utf-8')
          js = Path('assets/search-history.js').read_text(encoding='utf-8')
          link = '<script src="assets/search-history.js"></script>'
          if html.count(link) != 1:
              raise SystemExit(f'Expected one search-history link, found {html.count(link)}')
          if 'function aramaGecmisiKaydet' in html:
              raise SystemExit('Search-history function remains inline.')
          if 'function hitUrunleriHesapla' not in html:
              raise SystemExit('Boundary function disappeared from index.html.')
          required = ['aramaGecmisiKaydet', 'aramaGecmisiniGoster', 'aramaSec']
          for name in required:
              if name not in js:
                  raise SystemExit(f'Missing expected function/name: {name}')
          if not js.strip():
              raise SystemExit('Search-history module is empty.')
          print('Search-history extraction validation passed.')

      - name: Commit extracted module
        shell: bash
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add index.html assets/search-history.js
          git diff --cached --quiet && exit 0
          git commit -m 'refactor: extract search history module [refactor-bot]'
          git push origin HEAD:project-context
